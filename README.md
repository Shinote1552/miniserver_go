# URL Shortener Service

####  Мини-сервер для сокращения URL на Go с поддержкой PostgreSQL и in-memory хранилищ. Полностью контейнеризован с Docker.

## 🚀 Быстрый старт

```bash
# Справка по всем командам
make help
# Клонирование и запуск
make setup
make build
make run

# Тестирование
make test-ping      # Проверка работы
make test-curl      # Полное API тестирование
make test           # unit тесты
make cover          # покрытие
```

## 🛠️ Конфигурация
### Через флаги или переменные окружения:

```bash
SERVER_ADDRESS=:8080
BASE_URL=http://localhost:8080
DATABASE_DSN=postgres://user:pass@host:5432/db
JWT_SECRET_KEY=your-secret-key
```

## 📦 Основные возможности
* Сокращение URL и текстовые запросы
* JWT аутентификация с автоматической обработкой куки
* Два хранилища: PostgreSQL (основное) и in-memory
* Gzip сжатие ответов
* Контейнеризация с Docker Compose
* Менеджер транзакций с контролем уровней изоляции

## 🗄️ Хранилища
* PostgreSQL — основное хранилище с транзакциями
* File Storage — долгосрочное хранилище сервера в JSON-файле:
При остановке сервера данные выгружаются из БД в файл;
При запуске проверяет дубликаты и загружает данные обратно в БД;
Обеспечивает сохранность данных между перезапусками;
* In-Memory — аварийный вариант хранения в памяти сервера при недоступности PostgreSQL

## 📡 API Endpoints
### Публичные обработчики

#### `GET /ping`
- **Назначение**: Проверка соединения с базой данных (health check)
- **Ответы**:
  - `200 OK` - если БД доступна
  - `500 Internal Server Error` - если соединение с БД недоступно
- **Использование**: Мониторинг работоспособности сервиса

#### `GET /{id}`
- **Назначение**: Перенаправление по короткому идентификатору
- **Поведение**:
  - При успешном нахождении URL возвращает `307 Temporary Redirect` с заголовком `Location`
  - Если URL не найден - возвращает `400 Bad Request`
- **Особенности**: Увеличивает счетчик переходов для статистики

#### `GET /`
- **Назначение**: Обработчик корневого пути
- **Поведение**: Всегда возвращает `400 Bad Request`
- **Использование**: Обработка некорректных запросов к корню сервера

### Защищенные обработчики (требуют JWT аутентификации)

#### `POST /api/shorten`
- **Назначение**: Создание короткой версии URL
- **Формат запроса**: JSON `{"url": "https://example.com"}`
- **Ответы**:
  - `201 Created` - успешное создание, возвращает короткий URL в JSON
  - `409 Conflict` - при попытке создать дубликат URL (уже существует)
  - `401 Unauthorized` - при отсутствии или невалидной JWT 

#### `POST /api/shorten/batch`
- **Назначение**: Пакетное создание коротких URL
- **Формат запроса**: Массив объектов с `correlation_id` и `original_url`
- **Ответ**: Массив результатов с сохранением `correlation_id` для сопоставления
- **Особенности**: Атомарная обработка - либо все URL создаются, либо ни одного

#### `POST /`
- **Назначение**: Альтернативная версия сокращения URL (plain text)
- **Формат запроса**: URL в теле запроса (`text/plain`)
- **Ответ**: Короткий URL в виде текста
- **Логика**: Аналогична `/api/shorten`, но с упрощенным форматом

#### `GET /api/user/urls`
- **Назначение**: Получение всех URL текущего пользователя
- **Формат ответа**: Массив объектов json `{"short_url": "...", "original_url": "..."}`
- **Ответы**:
  - `200 OK` - с массивом URL пользователя
  - `204 No Content` - если у пользователя нет сохраненных URL
  - `401 Unauthorized` - при отсутствии валидной JWT куки
- **Особенности**: Данные строго идентифицированы по пользователям


## 🏗️ Архитектура и структура проекта

Проект реализован с четким разделением ответственности по слоям:

- **Domain Models**: `internal/domain/models/` - чистые бизнес-сущности, не зависящие от других слоев
- **Services**: `internal/services/` - бизнес-логика (use cases), работающая исключительно с доменными моделями
- **Handlers**: `internal/http/handlers/` - HTTP контроллеры
- **DTOs**:
- - `internal/http/dto/` - транспортные модели для HTTP слоя
- - `internal/repository/dto/` - модели репозитория для работы с БД
- **Repository** - слой работы с данными с реализацией интерфейсов, определенных в сервисном слое

**Преимущества архитектуры**:
- Интерфейсы как четкие контракты между слоями
- Изолированное тестирование компонентов с mock заглушками
- Возможность замены реализаций без изменения бизнес-логики
- Независимость доменного слоя от инфраструктурных деталей

## ⚠️ Текущие ограничения
- Нет rate limiting
- Базовое логирование (только middleware)
- Упрощенная обработка ошибок

## 🚀 Планируемые улучшения
- SingleFlight
- errgroup
- Структурированное логирование
- Rate limiting
- Улучшенная обработка ошибок
- Доокументация swagger api


## Доступные флаги командной строки

| Флаг                  | Описание                          | Пример значения                  |
|-----------------------|-----------------------------------|----------------------------------|
| `-server-address`     | Адрес и порт сервера              | `-server-address=":8080"`        |
| `-base-url`           | Базовый URL для ссылок            | `-base-url="https://example.com"`|
| `-file-storage-path`  | Путь к файлу хранилища URL        | `-file-storage-path="/data/urls.json"`|
| `-database-dsn`       | DSN для подключения к PostgreSQL  | `-database-dsn="postgres://user:pass@localhost:5432/db"`|
| `-jwt-access-expire`  | Время жизни Access токена         | `-jwt-access-expire="15m"`       |

## Переменные окружения

| Переменная              | Описание                          | Пример значения                          |
|-------------------------|-----------------------------------|------------------------------------------|
| `SERVER_ADDRESS`        | Адрес сервера                    | `:8080` или `0.0.0.0:9000`              |
| `BASE_URL`              | Базовый URL для ссылок           | `https://short.example.com`              |
| `FILE_STORAGE_PATH`     | Путь к файлу хранилища           | `/var/data/urls.json`                    |
| `DATABASE_DSN`          | PostgreSQL DSN строка            | `postgres://user:pass@host:5432/dbname`  |
| `JWT_SECRET_KEY`        | Секретный ключ для JWT           | `base64-encoded-32-byte-key`             |
| `JWT_ACCESS_EXPIRE`     | Время жизни Access токена        | `15m` (15 минут)                         |


## Профили Docker Compose в проекте:

* ### 🐘 Основной (default)
* * `docker compose up`
* * Порт: 8080
* * Хранилище: PostgreSQL
* * Назначение: Продакшен-режим с полноценной БД

* ### 🚀 In-Memory (тестовый)
* * `docker compose --profile inmemory up`
* * Порт: 8081
* * Хранилище: In-Memory (без БД)
* * Назначение: Тестирование, разработка, быстрый старт

* ### 🔄 Fallback (In-Memory (но с ошибкой БД))
* * `docker compose --profile fallback up`
* * Порт: 8082
* * Хранилище: In-Memory (при ошибке подключения к БД)
* * Назначение: Демонстрация отказоустойчивости

